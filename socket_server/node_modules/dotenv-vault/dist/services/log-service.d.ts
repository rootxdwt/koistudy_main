interface LogServiceAttrs {
    cmd: any;
}
declare class LogService {
    cmd: any;
    constructor(attrs?: LogServiceAttrs);
    get pretextLocal(): string;
    get pretextRemote(): string;
    plain(msg: string): void;
    local(msg: string): void;
    remote(msg: string): void;
}
export { LogService };
